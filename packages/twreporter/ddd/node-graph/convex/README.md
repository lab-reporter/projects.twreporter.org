# Convex

Convex backend files for the node graph app.

## Files

- `auth.config.ts`: auth provider config
- `lib/helpers.ts`: shared auth-wrapped helpers

## Workflow

```bash
pnpm convex:dev
```

Run Convex dev when changing the schema or backend functions so generated files stay in sync.
